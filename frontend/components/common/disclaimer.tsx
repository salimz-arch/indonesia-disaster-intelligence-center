/** Disclaimer §41 — wajib di semua halaman publik. */
export function Disclaimer() {
  return (
    <footer className="border-t border-idic-border pt-4 text-[11px] leading-relaxed text-slate-600">
      This platform is an information and monitoring tool. It does not replace
      official disaster warnings, emergency services, or government authorities.
      Always verify critical information with official sources — BMKG
      (bmkg.go.id) dan BNPB (bnpb.go.id).
    </footer>
  );
}
